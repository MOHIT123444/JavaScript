let score =false; 
console.log(typeof score);
console.log(typeof(score)); //This is a method // both forms of type  of are valid. 

let ValueInNumber = Number(score); // this is used to convert string into number.   


console.log("this is the: ", typeof  ValueInNumber);

console.log(ValueInNumber);

let ValueInString = String(ValueInNumber); // this is used to convert number into string.
let ValueInBoolean = Boolean(ValueInString); // this is used to convert string into boolean.






